# 统一行尾，避免 Windows 下 CRLF / LF 混用导致的无意义 diff
* text=auto eol=lf

# Windows 脚本保持 CRLF
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做行尾转换
*.png binary
*.jpg binary
*.ico binary
*.pdf binary
