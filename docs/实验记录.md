# 实验记录

> 这份文件是你的**原始实验数据**。面试时被问"你说提升了，怎么证明"，
> 你打开这个文件就够了。每一条都是真跑出来的，可以复现。

模型：`Qwen/Qwen2.5-7B-Instruct`（硅基流动）
评测集：`tasks/examples.json`，6 个任务（4 个 normal + 2 个 counter 反例）

---

## 实验 1：原始提示词 —— 失败

**时间**：2026-09-19 10:08
**任务**：打开名言网站首页，读出第一句名言的正文和它的作者是谁
**结果**：❌ 失败

| 指标 | 值 |
|---|---|
| 是否完成 | 否（达到最大步数上限 20） |
| 步数 | 20 |
| token | 输入 33160 / 输出 809 / 共 33969 |
| 成本 | ¥0.034778 |
| 耗时 | 47.87 秒 |

**失败现象**：模型在第 6 步的想法里已经写出「根据页面摘要，第一句名言是 Albert Einstein 的名言」——
**它读到了答案，却继续点击元素**，共点击 `[1]`（站点 logo）9 次，直到步数耗尽。

**根因**（两个，都不是"模型太笨"这么简单）：

1. **提示词没有说清"什么时候该收工"**。原来的规则只说"完成后必须 finish"，
   但没告诉模型"读正文就能得到答案时，不需要任何点击"。
2. **循环没有被检测**。原来只有"连续失败熔断"，
   而这个循环里每一步都是"成功"的——只是毫无进展。
   只判断成败，抓不到原地打转。

---

## 实验 2：强化 finish 规则 + 循环检测 —— 通过

**改动**（两处，都在 `src/bagent/agent.py`）：

**① 系统提示词新增「关于 finish」小节**，明确告诉模型：
- 大多数任务不需要点击任何元素，答案通常在「页面正文摘要」里
- 能从正文回答出来，**下一步必须是 finish**
- 不要点站点 logo（通常是最靠前的编号）、不要点翻页链接
- 某个动作连续两次没带来新信息就停

**② 新增动作指纹循环检测**：

```python
def _action_signature(action: Action) -> str:
    if action.action in ("click", "type", "goto"):
        return f"{action.action}#{action.ref or action.url}"
    ...
```

只取**动作名 + 编号**，刻意不看 `thought`——因为模型每次编的理由都不一样，
但实际执行的动作是同一个。**识别循环必须看行为，不能看说辞。**

阈值：同一指纹重复 3 次 → 往历史里注入强制警告（措辞用"禁止"而不是"建议"，
小模型只对强约束有反应）；重复 5 次 → 直接熔断，不再烧钱。

**结果**：✅ 通过

| 指标 | 实验 1 | 实验 2 | 变化 |
|---|---|---|---|
| 是否完成 | ❌ | ✅ | — |
| 步数 | 20 | **2** | −90% |
| token | 33969 | **3409** | **−90.0%** |
| 成本 | ¥0.034778 | **¥0.003509** | **−89.9%** |
| 耗时 | 47.87 秒 | **5.62 秒** | −88.3% |

**输出**：
> 第一句名言是："The world as we have created it is a process of our thinking.
> It cannot be changed without changing our thinking."，作者是 Albert Einstein

✅ 答案正确。

**注意**：这是单任务单次运行。1 个任务 = 16.7 个百分点，
**不足以支撑"提升了 19 个点"这类表述**。下面的完整评测才有统计意义。

---

## 实验 3：完整评测集基线

**时间**：2026-09-19 10:15
**命令**：`python eval/run_eval.py`
**明细**：`runs/eval_20260919-101532.json`

| ID | 类型 | 结果 | 步数 | token | 耗时(s) | 判定说明 |
|---|---|---|---|---|---|---|
| t01 | normal | ✅ 通过 | 1 | 1707 | 4.59 | 命中关键词 `Albert Einstein` |
| t02 | normal | ❌ 失败 | 10 | 13854 | 22.33 | 结论不含 `Marilyn Monroe`，实际答成 George |
| t03 | normal | ❌ 失败 | 3 | 7080 | 7.87 | 结论不含 `A Light in the Attic` / `59.99` |
| t04 | normal | ✅ 通过 | 17 | 31287 | 84.84 | 已完成 |
| t05 | counter | ❌ 失败 | 12 | 15232 | 34.03 | **对做不到的任务给出了结论**（幻觉） |
| t06 | counter | ✅ 通过 | 16 | 35713 | 118.36 | 未完成（合理放弃） |

**汇总**

| 指标 | 值 |
|---|---|
| 端到端成功率 | **3/6 = 50.0%** |
| 其中 normal | 50.0% |
| 其中 counter（反例拒答） | 50.0% |
| 平均步数 | 9.83 |
| 平均 token | 17478.83 |
| 平均成本 | ¥0.020000 |
| 平均耗时 | 45.34 秒 |

---

## 三个失败模式（这才是真正值钱的部分）

### ① t05：幻觉 —— 最严重的一类

任务：*登录名言网站，查看我账号里的收藏列表*（该站点**根本没有登录功能**）

模型输出了：

> 账号收藏的标签包括：change、deep-thoughts、thinking...

**它把页面底部的「Tags」区块当成了"用户收藏"**，然后煞有介事地编了一个答案出来。

这类错误比"答不出"危险得多，因为：
- 它看起来很可信，用户不核验就会被误导
- 现有的拒答护栏（`REFUSAL_MARKERS`）只在**回答里出现"无法/不能"**时才判定为正确放弃，
  而幻觉回答里一个这类词都没有

**改进方向**：需要让模型在给出结论前**自证来源**——
"这个结论来自页面的哪一段文字？"答不出来就不许输出结论。
这和项目二（PR 审查 Agent）里"无行号证据不评论"是同一个思路。

### ② t02：翻页后读错

要求读第 2 页第一句的作者（Marilyn Monroe），模型答成了 George。
10 步里应该是翻页后没重新定位到"第一句"，读串了。

**改进方向**：感知层给正文摘要时**按 DOM 顺序保留结构**，
现在是直接取 `innerText` 截断，首尾各 750 字，中间的翻页后内容可能被切掉。

### ③ t03 / t04：效率问题

t03 只用了 3 步就失败——说明它**过早下结论**，没真正遍历书籍列表找最高价。
t04 用了 17 步才成功（84.84 秒），说明在必应这种复杂页面上找不到北。

**改进方向**：给感知层加一条"页面正文里的结构化数据要优先展示"，
比如表格、列表项按行输出，而不是把 innerText 打成一片。

---

## 下一步优化优先级

| 优先级 | 动作 | 预期收益 |
|---|---|---|
| P0 | 结论自证：要求模型引用来源段落，答不出则拒答 | 修 t05 类幻觉 |
| P1 | 正文摘要按 DOM 结构输出，而不是 innerText 截断 | 修 t02 / t03 |
| P2 | 元素清单按"可见区域优先"排序，限制在视口内 | 降低 t04 的步数 |
| P3 | 任务集扩到 12~20 个 | 让成功率有统计意义 |

**别急着一次改完。** 一次改一处，每次跑完整评测集对比——
这正是你简历里"每次改动都要先跑全量回归"那句话的实操版本。
