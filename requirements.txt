# 运行依赖
openai>=1.40.0
playwright>=1.47.0
python-dotenv>=1.0.1
pydantic>=2.7.0
rich>=13.7.0
httpx>=0.27.0

# 开发/测试
pytest>=8.0.0

# ---------------------------------------------------------------------------
# 可选：以下都不是跑主循环的必需品，按需取消注释
# ---------------------------------------------------------------------------
# LangGraph 引擎（--engine langgraph）
# langgraph>=0.2.0
# langchain-core>=0.3.0
#
# FastAPI 服务化（uvicorn bagent.api:app）
# fastapi>=0.115.0
# uvicorn>=0.30.0
