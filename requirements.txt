# 运行依赖
openai>=1.40.0
playwright>=1.47.0
python-dotenv>=1.0.1
pydantic>=2.7.0
rich>=13.7.0
httpx>=0.27.0

# 开发/测试
pytest>=8.0.0

# 服务化（uvicorn bagent.api:app）——**不是可选项**：
# 镜像里就是要跑这个 API，Dockerfile 只装本文件，
# 所以 uvicorn / fastapi 必须在这里声明。
# 踩过的坑：之前它们是注释状态，本地 .venv 里是手工装的，
# 于是本机跑得通、镜像里却 ModuleNotFoundError。
fastapi>=0.115.0
uvicorn>=0.30.0

# ---------------------------------------------------------------------------
# 可选：以下不是跑主循环的必需品，按需取消注释
# ---------------------------------------------------------------------------
# LangGraph 引擎（--engine langgraph）。
# 容器不跑图引擎，所以镜像不装；CI 里单独 pip install 给测试用。
# langgraph>=0.2.0
# langchain-core>=0.3.0
